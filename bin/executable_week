#!/bin/bash

if [ "$#" -ne 3 ]; then
    echo "Usage: $0 <week> <month> <year>"
    exit 1
fi

week=$1
month=$2
year=$3

first_day=$(date -u -d "$year-$month-01 +$((week-1)) weeks" '+%Y-%m-%d')
last_day=$(date -u -d "$first_day + 6 days" '+%Y-%m-%d')

echo "$first_day" "$last_day"
