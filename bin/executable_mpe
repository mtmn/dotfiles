#!/bin/bash

TMPFILE=$(mktemp)
trap "rm -f $TMPFILE" EXIT

mpc playlist --format '%file%' >"$TMPFILE"

$EDITOR "$TMPFILE"

mpc clear

while IFS= read -r file; do
	if [ -n "$file" ]; then
		mpc add "$file"
	fi
done <"$TMPFILE"

mpc playlist
mpc play
